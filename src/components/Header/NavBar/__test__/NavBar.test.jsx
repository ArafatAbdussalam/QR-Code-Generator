import { render } from "@testing-library/react";
import '@testing-library/jest-dom'

import NavBar from "../NavBar";



// pass auth hook

// describe(``, () => {

// test(`should render navigation role`, () => {
//     const component = render(<NavBar />)
//     const selectedMode = component.getByRole("navigation")
//     expect(selectedMode).toBeInTheDOcument()
// })

//     test(`should render text button child components with login and signup name when user is not authenticated`, () => {
//         const component = render(<NavBar />)
//     })

//     test(`should render text button child component with logout name when user is authenticated`, () => {
//         const component = render(<NavBar />)
//     })

//     test(`should not render text button child components with login and signup name when user is not authenticated`, () => {
//         const component = render(<NavBar />)
//     })

//     test(`should not render text button child components with logout name when user is authenticated`, () => {
//         const component = render(<NavBar />)
//     })
// })